//! osti: a terminal, keyboard-driven, modal tool for composing music.

// `main` can't meaningfully be `const`; it's never called in a const context.
#[allow(clippy::missing_const_for_fn)]
fn main() {}
